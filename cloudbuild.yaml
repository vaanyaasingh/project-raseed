steps:
  - name: 'gcr.io/cloud-builders/docker'
    args:
      - 'build'
      - '-t'
      - 'gcr.io/$PROJECT_ID/project-raseed:$COMMIT_SHA'
      - '-f'
      - 'backend/Dockerfile'
      - './backend'

images:
  - 'gcr.io/$PROJECT_ID/project-raseed:$COMMIT_SHA'
